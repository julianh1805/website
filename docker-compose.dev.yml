version: '3.8'
services:
  website-app-dev:
    container_name: website-app-dev
    build:
      context: .
      dockerfile: docker-images/Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - ./src:/app/src
